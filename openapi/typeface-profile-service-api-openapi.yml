openapi: 3.2.0
info:
  title: Content Workflow Profile Service API
  description: This template will guide you through the API endpoints to get workflows, get workflow steps, apply workflow to document, update document workflow status, update document workflow, update document assignees.
  version: 1.0.0
  contact: {}
servers:
- url: https://api-us.typeface.ai
security:
- bearerAuth: []
tags:
- name: Profile Service
paths:
  /profile-service/accounts/{accountId}/workflows:
    get:
      summary: Get Workflows
      description: Retrieve the list of workflows associated with a specific account.
      operationId: getWorkflows
      parameters:
      - name: accountId
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: A list of workflows associated with the account.
          content:
            application/json:
              schema:
                type: object
                properties:
                  workflows:
                    type: array
                    items:
                      $ref: '#/components/schemas/EntitySchema'
      security:
      - bearerAuth: []
      tags:
      - Profile Service
  /profile-service/accounts/{accountId}/workflows/{workflowId}/workflow-steps:
    get:
      summary: Get Workflow Steps
      description: Retrieve the steps/statuses of a specific workflow.
      operationId: getWorkflowsSteps
      parameters:
      - name: accountId
        in: path
        required: true
        schema:
          type: string
      - name: workflowId
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: A list of steps/statuses in the specified workflow.
          content:
            application/json:
              schema:
                type: object
                properties:
                  workflowSteps:
                    type: array
                    items:
                      $ref: '#/components/schemas/EntitySchema'
      security:
      - bearerAuth: []
      tags:
      - Profile Service
components:
  schemas:
    EntitySchema:
      type: object
      properties:
        workspaceId:
          type: object
          properties:
            id:
              type: integer
          required:
          - id
        id:
          type: object
          properties:
            id:
              type: integer
          required:
          - id
        name:
          type: string
        sourcePath:
          type: string
        ingestionSource:
          type: string
        properties:
          type: object
          properties:
            orderedWorkflowStepIds:
              type: array
              items:
                type: integer
        createdAt:
          type: string
          format: date-time
        modifiedAt:
          type: string
          format: date-time
        createdBy:
          type: string
        modifiedBy:
          type: string
        renditionId:
          type: string
        ref:
          type: string
        entitySubType:
          type: string
        sourceId:
          type: string
        sourceType:
          type: string
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
x-readme:
  explorer-enabled: true
  proxy-enabled: true
